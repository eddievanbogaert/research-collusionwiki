# Live backlog

The 16 dump cuts in [`analysis/next-angles.md`](../analysis/next-angles.md) are **done**. That file is the original brief, not the work queue. Reproduce those notes with `analyze.py` / `deeper.py` / `backlog.py`.

What belongs here now is only work that (a) this zip still under-answers, (b) is off-dump but already named as a lead, or (c) is distillation / watch. Everything else is out.

## Do next (this repo)

1. **One-pager.** [`brief.md`](brief.md). Keep it current if a hunt or OpenAI statement changes the bounds. Do not grow it into a fourth technical note.
2. **OECD vintage.** Cut 6 sampled DataUSA and said 9.91 vs 9.90 is the real wrong-consensus fight. Pin the public OECD/Power BI number against what the board locked in. Dump-bounded; no bypass replay.
3. **One more episode** only if the one-pager needs it: cashiers or sector 61-62 at clothing/grocery depth. Do not read every named family.

## Do next (read-only, off-dump)

Highest value first. History and RecentChanges only. No saves, no “editability tests,” no paste-id guessing.

4. **wiki4d before 4 Sep 2026.** Same farm, named once in this zip, zero stored revisions. Confirm May–July agent dialect vs the 4 Sep `CollusionWikiProbe` contamination.
5. **ludism.org sandbox, 26 May window.** Titles and payloads already in public RC; this zip has `/16` only. Record overlap with DSE `FederalData*` / `max.gov` language. Do not claim exact-IP identity unless a public log supports it.
6. **One SiteList Active pass** (Meatball, lua-users, TextEditors, ToothyWiki): May–July 2026 rows matching `OpenAI` / `pums_5` / `county.json` / `ZZZ`. Stop after a negative or a contaminated hit. Outdated SiteList is a later pass — dormant CGI is DSE-like, but it is a long list.

## Watch, do not staff

7. **OpenAI on-record review** of collusion.wiki. Coverage note, not a dump finding.
8. **Reddit** if a real thread appears. Still none indexed as of 5 Sep. Post-news threads are contamination, same as wiki4d 4 Sep.
9. **Colony / HN leads** (ApchemWiki July tail, linuxiarz pastes, RubyGems account, public counters). Treat as a lead list. Do not scrape strangers’ pastes. Do not query restricted shorteners.

## Does not belong

- Re-running the four-wiki scrape we already have.
- Full identity graph / true sandbox count (needs unredacted IPs).
- 2^32 seed search.
- GET-save, XSS, tunnel, SNI/NO_PROXY, or “confirm it still writes” tests.
- Merging this board with Hugging Face / Artifactory / ExploitGym.
- Enumerating pastebin IDs or writing to Vanderbilt / operators.
- A local explorer that competes with collusion.wiki (`reader.py` is enough).
- Absorbing every proxy hostname into the one-pager.

## How to proceed

Dump analysis has diminishing returns. The next useful object is **the one-pager, then wiki4d history**, then stop unless a lead is a May–July hit with the dump’s dialect.

Open questions this repo will not close from `data/` alone: how they found DSE; whether 21–26 June OpenAI-IP browsing caused the 22 June stop; chain-of-thought; how many other boards exist.
